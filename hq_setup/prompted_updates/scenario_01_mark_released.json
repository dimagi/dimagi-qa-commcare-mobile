{
  "_comment": "Master Mobile Plan (2026) > Prompted Updates > Prompted Update Scenario 1 ('Optional CCZ update end-to-end'). Run once via `python scripts/hq_client.py hq_setup/prompted_updates/scenario_01_mark_released.json` after Setup 2/3 have run (latest build In Test, Prompt Updates to CommCare/App = On) and after flows/prompted_updates/scenario_01_optional_ccz_update.yaml's first login/logout/login checkpoint - releasing the latest build is what makes the optional 'New version of the application is available' prompt appear on the flow's next login. saved_app_id should be the SAME build that Setup 2 marked In Test. UPDATE (2026-08-20): app_id/saved_app_id filled in (same latest build as setup_02_mark_in_test.json's own UPDATE comment - see that file for the full citation). CAVEAT (unchanged): this action genuinely must run BETWEEN the flow's own logout and re-login, which scripts/run_suite.py's single --hq-setup-before-everything model can't do within one continuous Maestro build - see this repo's own README/FRAMEWORK docs on running hq_setup steps manually via `python scripts/hq_client.py <file>` at the right point, matching the header's '=== HQ pre-step runs here ===' marker.",
  "actions": [
    {
      "type": "mark_build_status",
      "app_id": "cdfa6c85eb594b23b0c08729cd2beff1",
      "saved_app_id": "c5e73a9735154c4fafa87667a2666f69",
      "is_released": true
    }
  ]
}
